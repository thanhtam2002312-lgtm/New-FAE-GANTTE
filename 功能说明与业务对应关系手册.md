# FAE 项目管理与甘特图协同系统 · 功能说明与业务对应关系手册

> **文档版本**：v3.2  
> **适用系统**：FAE Project & Gantt Collaborative Management Platform  
> **编写目的**：全面梳理系统中的业务实体、核心关键词、映射对应关系、计算法则、功能模块与边缘异常处理逻辑，供业务审核、逻辑验收与 Bug 排查。

---

## 目录
1. [系统数据架构与四级层级关系](#1-系统数据架构与四级层级关系)
2. [核心业务关键词与术语字典](#2-核心业务关键词与术语字典)
3. [Excel 导入导出映射规则与防截胡算法](#3-excel-导入导出映射规则与防截胡算法)
4. [核心功能模块与交互细节](#4-核心功能模块与交互细节)
5. [数据持久化与容错迁移机制](#5-数据持久化与容错迁移机制)
6. [潜在 Bug 审查与边缘场景排查清单](#6-潜在-bug-审查与边缘场景排查清单)

---

## 1. 系统数据架构与四级层级关系

系统采用严格的 **四级树状层级模型（Customer -> Project -> PN -> Task）**，满足半导体/电子元器件原厂与代理商 FAE（现场应用工程师）跟进客户项目的业务链路。

```
[Level 1: Customer (客户)]
    │   ├─ 客户中文名 (nameZh) / 客户英文名 (nameEn)
    │   ├─ 客户代码 (customerCode)
    │   ├─ 负责销售 (Sales EN / Sales CN)
    │   └─ 客户研发对接人 (customerRd)
    │
    └── [Level 2: Project (项目/机型)]
            │   ├─ 项目名称 (name)
            │   ├─ 预计量产时程 (mpSchedule)
            │   └─ 项目总金额 (ltrAmt) ← 由子 PN 动态汇总求和
            │
            └── [Level 3: PN (料号 / 零件型号 / Part Number)]
                    │   ├─ 料号名称 (name)
                    │   ├─ 品牌产品线 (productLine)
                    │   ├─ 当前商机状态 (status: Leads/NBO/DIN/DFIN/DWIN/DLOST)
                    │   ├─ 原厂报备状态 (drStatus: DR#)
                    │   ├─ 立项日期 (socketCreateDate)
                    │   ├─ Socket 总生命周期金额 (socketTotalLtrAmt)
                    │   ├─ 细分市场 (marketSegment)
                    │   ├─ 渠道通路 (channelOk: Yes/No)
                    │   ├─ 业务备注 (remark)
                    │   └─ 历史跟进进展更新 (updated: FAE/PM/Sales 最新日志)
                    │
                    └── [Level 4: Task (甘特图时间线活动任务)]
                            ├─ 任务名称 (name)
                            ├─ 任务状态 (status: standard/risk/done/waiting)
                            ├─ 起始日期 (startDate: YYYY-MM-DD)
                            ├─ 截止日期 (endDate: YYYY-MM-DD)
                            ├─ 责任人 (owner)
                            └─ 受阻原因/风险瓶颈 (blocker)
```

### 级联关系与自动聚合规则：
1. **删除级联**：删除一个客户会级联删除其下属的所有项目、料号与任务；删除项目或料号同理。
2. **金额聚合计算 (`calculatePnTotalLtr`)**：
   - 规则：Project 的 `ltrAmt` 不需要手动维护，由其下属所有 PN 的 `socketTotalLtrAmt` 自动数值解析、统一换算（支持 `$`、`¥`、逗号、`K`、`M` 缩写）并求和展示。
3. **状态与进展隔离**：
   - **`PN.updated`**：存放来自 Excel 的技术与销售跟进文本（如 Weekly update、Call Report 等），属于静态多行更新记录。
   - **`PN.tasks`**：仅存放带有起始日期、截止日期的甘特图时间线真实任务，绝不让冗长文字污染时间轴。

---

## 2. 核心业务关键词与术语字典

### 2.1 基础与商务信息关键词
| 关键词 (中文) | 关键词 (英文) | 代码对应字段 | 数据类型 | 业务释义与规范 |
| :--- | :--- | :--- | :--- | :--- |
| **客户中文名** | Socket Customer Chinese Name | `Customer.nameZh` | `string` | 客户的正式中文注册名称或常用中文简称（如 "特斯拉"、"比亚迪"） |
| **客户英文名** | Customer English Name | `Customer.nameEn` | `string` | 客户的国际英文名（如 "Tesla"、"BYD"） |
| **客户代码** | Customer Code | `Customer.customerCode` | `string` | 原厂 ERP 或 CRM 中的客户编码（如 "TSLA001"） |
| **业务英文** | Sales EN | `Customer.salesEn` | `string` | 负责该客户商务的英文名或工号（如 "Elon M."） |
| **业务中文** | Sales CN | `Customer.salesCn` | `string` | 负责该客户商务的中文名（如 "马斯克"） |
| **客户研发窗口** | Customer R&D / Account RD | `Customer.customerRd` | `string` | 客户方的硬件/软件/系统研发工程师或项目经理对接窗口 |
| **渠道状态** | Channel OK | `PN.channelOk` | `"Yes" \| "No"` | 代理商渠道是否畅通/有无报备冲突，枚举值仅限 Yes 或 No |
| **业务备注** | Remark | `PN.remark` | `string` | 商务、交付或技术配合层面的辅助说明 |

---

### 2.2 技术、产品与项目关键词
| 关键词 (中文) | 关键词 (英文) | 代码对应字段 | 数据类型 | 业务释义与规范 |
| :--- | :--- | :--- | :--- | :--- |
| **细分市场** | Market (Segment) | `PN.marketSegment` | `string` | 客户项目所处的行业应用场景，如 Auto（汽车）、Industrial（工控）、Consumer（消费电子）、Medical（医疗）等 |
| **项目名称** | Project name | `Project.name` | `string` | 客户内部的整机/主板/模组项目代号（如 "Model Y 智能座舱项目"） |
| **产品线** | Brand (Product line) | `PN.productLine` | `string` | 具体的芯片/器件产品线（如 MCU、Power、Sensor、Analog 等） |
| **料号** | P/N (Part Number) | `PN.name` | `string` | 推广设计的具体器件完整型号（如 "STM32F407VGT6"、"1234567-00-A"） |
| **报备状态** | DR status (Design Registration) | `PN.drStatus` | `string` | 原厂的设计注册编号或审批状态（如 "DR-2026-889"、"Approved"） |
| **立项日期** | Socket Create date | `PN.socketCreateDate` | `string` | 在 CRM/ERP 录入此 Socket 商机的时间（格式归一为 YYYY-MM-DD） |
| **预计量产时程**| MP Schedule (Mass Production) | `Project.mpSchedule` | `string` | 客户整机预计进入量产（SOP/MP）的日期或季度月份 |
| **Socket 潜在金额**| Socket Total LTR AMT | `PN.socketTotalLtrAmt` | `string` | 该料号在项目全生命周期的预估总营收金额（LTR = Life Time Revenue） |
| **项目总金额** | Project Total LTR AMT | `Project.ltrAmt` | `string` | 当前项目下所有料号 LTR 金额的总和 |
| **进展更新** | Updated | `PN.updated` | `string` | 历次 FAE/PM/Sales 会议纪要、技术支持进展、Call Report |
| **活动任务** | Activity / Tasks | `PN.tasks` | `Task[]` | 在甘特图时间轴上排期推进的具体研发/测试任务 |

---

### 2.3 状态枚举与视觉映射

#### 1. 商机阶段状态 (`PN.status: PNStatus`)
遵循原厂标准 Sales Pipeline 阶段推进流：
- `Leads` (潜在商机 / 初步接触) → 浅蓝灰
- `NBO` (New Business Opportunity / 方案选型确认) → 紫色
- `DIN` (Design-In / 客户开始导入设计与打样) → 亮蓝色
- `DFIN` (Design-Win Finalizing / 客户验证通过，待商务锁定) → 琥珀橙
- `DWIN` (Design-Win / 商务下单，成功赢得设计锁定) → 翠绿色
- `DLOST` (Design-Lost / 丢单或竞品替代) → 玫瑰红

#### 2. 甘特图任务状态 (`Task.status: TaskStatus`)
- `standard` (正常进行中) → 品牌主题色高亮
- `risk` (存在风险 / 严重受阻) → 琥珀橙/深红高亮，并激活 `blocker` 悬浮提示气泡
- `done` (已完成验收) → 翠绿色完成标
- `waiting` (等待前置条件 / 挂起) → 柔和半透明冷灰

---

## 3. Excel 导入导出映射规则与防截胡算法

系统内置强大的 `excelService` 引擎，负责双向 Excel 解析与规范导出。

### 3.1 导出标准规范（18 根标准列定义）
导出时严格固定顺序，自动设置列宽（中文字符计 2、英文字符计 1）、自适应行高、冻结首行表头、微软雅黑色彩边框与斑马纹交替底色：

1. `Sales EN` (宽 14, 居中)
2. `Sales CN` (宽 12, 居中)
3. `Customer Code` (宽 16, 居中)
4. `Customer English Name` (宽 25, 居左)
5. `Socket Customer Chinese Name` (宽 28, 居左)
6. `Customer R&D` (宽 16, 居中)
7. `Market (Segment)` (宽 18, 居中)
8. `Project name` (宽 26, 居左)
9. `Brand (Product line)` (宽 20, 居左)
10. `P/N` (宽 22, 居左)
11. `Current Status` (宽 15, 居中)
12. `DR status` (宽 14, 居中)
13. `Socket Create date` (宽 18, 居中)
14. `MP Schedule` (宽 16, 居中)
15. `Socket Total LTR AMT` (宽 20, 居右)
16. `Channel OK` (宽 13, 居中)
17. `Remark` (宽 26, 居左)
18. `Updated` (宽 45, 居左, 自动换行)
19. `Activity` (宽 50, 居左, 自动换行, 导出甘特图格式: `YYYYMMDD-YYYYMMDD：[责任人] 任务名`)

---

### 3.2 导入智能多别名模糊匹配字典 (Anti-Collision Algorithm)

为解决不同客户/销售/PM 制作的 Excel 表头千奇百怪、中英文缩写混杂、甚至同音同义问题，解析器采用 `cleanKey(k)`（剥离所有空格、换行、破折号、括号、冒号并小写化）配合正则别名库进行匹配：

| 标准系统字段 | Excel 表头别名池（包含但不仅限于） | 特殊防截胡与过滤算法 |
| :--- | :--- | :--- |
| **客户中文名** | `Socket Customer Chinese Name`, `Customer Chinese Name`, `Customer CN`, `客户中文名`, `客户中文名称`, `客户名称中文`, `客户` | 若表头仅为 `Customer`，检测单元格内容：含中文字符则归入 `nameZh`，纯英文则归入 `nameEn`。 |
| **客户英文名** | `Customer English Name`, `Customer English`, `Customer EN`, `Customer Name EN`, `客户英文名`, `客户英文名称` | 相互保底：若其中一项为空而另一项存在，自动相互填充防空。 |
| **客户研发窗口** | `Customer R&D`, `Account RD`, `Customer RD Window`, `客户RD`, `客户研发`, `研发负责人`, `研发窗口`, `RD Window` | **防截胡机制**：坚决排除 `DR Status`、`DR#`、`Board Name`、`Account Rep`（销售代表）。 |
| **客户代码** | `Customer Code`, `Customer ID`, `Customer No`, `客户代码`, `客户编号` | 支持中英代码清洗。 |
| **销售人员** | `Sales EN`, `Sales Rep EN`, `业务英文`, `销售英文` / `Sales CN`, `业务中文`, `销售中文` | 双向分离提取。 |
| **项目名称** | `Project name`, `Project na`, `项目名称`, `项目名` | **防截胡机制**：排除包含 `Date`、`Create`、`Total`、`AMT` 的金额与日期列。 |
| **料号 P/N** | `P/N`, `Part Number`, `Part No`, `Part`, `料号`, `型号`, `产品型号`, `零件号` | 正则优先提取有效型号，空值填充 "默认PN"。 |
| **预计量产时程**| `MP Schedule`, `MP Date`, `Mass Production Schedule`, `量产时间`, `量产时程` | 排除 `PP Schedule`；统一通过 `normalizeDateStr` 规范为 YYYY-MM-DD。 |
| **立项日期** | `Socket Create date`, `Socket Create`, `Create Date`, `创建日期`, `立项日期` | 自动标准化日期。 |
| **生命周期金额**| `Socket Total LTR AMT`, `Project Total LTR AMT`, `Total LTR AMT`, `总金额`, `金额` | 兼容 `$`, `¥`, `,`, `K`, `M` 货币与倍率解析。 |
| **渠道状态** | `Channel OK`, `渠道是否ok`, `渠道` | 自动判断 `No/no/N/否/false/0` 转为 `"No"`，其余默认转为 `"Yes"`。 |

---

### 3.3 产品线专属四级防截胡提取算法 (`extractProductLineFromRow`)

**业务痛点**：许多原始 Excel 表中同时存在 `Brand`（常被填为 NA 或原厂名）和 `Brand (Product line)` / `Product line`。若直接匹配包含 `Brand` 的列，产品线会被错误赋值为 NA。

**系统解决方案**：
1. **第一优先级**：严格全字匹配包含 `product line` 或 `产品线` 的专用列（如 `brandproductline`、`productline`、`产品线`、`品牌产品线`）。
2. **第二优先级**：扫描同时包含 `product` 与 `line` 的组合表头。
3. **第三优先级**：匹配行业标准缩写 `PL`。
4. **第四优先级**：仅当以上完全未找到，才退回到纯 `Brand` 表头，且**强制执行 `isDummyValue` 校验**。若其值为 `NA`、`N/A`、`#N/A`、`None`、`null`、`-`、`未知`，直接丢弃，绝不污染系统！

---

### 3.4 繁体转简体与脏数据过滤机制

1. **繁简自动识别与转换 (OpenCC)**：
   - 系统自动对 Excel 每一行的字符进行检测，若发现繁体字（如 "客戶"、"專案"、"測試"），自动通过 `OpenCC` 引擎一键转换为规范简体中文存入数据库，同时保留原始数据备份 `rawData`。
2. **无效占位符过滤 (`isDummyValue`)**：
   - 过滤词典：`na`, `n/a`, `#n/a`, `nan`, `none`, `null`, `-`, `--`, `/`, `\`, `无`, `暂无`, `未知`, `未定义`。
   - 命中以上词汇的单元格一律视为空白，防止出现 "NA" 产品线或无效数据。

---

### 3.5 Updated 与 Activity (甘特图任务) 彻底解耦隔离

- **Updated 归集池**：
  - 自动聚合来自 `Activity (FAE/PM/Others)`、`Activity (Sales)`、`Updated`、`最新进展` 的文本日志；
  - 遇到技术与销售跟进时，自动结构化拼接为：
    ```text
    [FAE/PM 进展]
    2026/01/10 Call Report: 原理图测试完成...

    [Sales 进展]
    2026/01/12 商务价格谈判确认...
    ```
- **Activity (甘特图任务) 纯净池**：
  - 仅当单元格为纯任务清单格式（如 `20260101-20260115：[张工] 原理图评审`）且不含 Call Report / Weekly update 关键字时，才解析为甘特图 Task；
  - 启动加载时内置**数据清洗管道 (`sanitizeLoadedCustomers`)**，若发现历史存量中有误存入任务栏的跟进纪要，自动清洗转移至 `updated`，恢复时间轴整洁。

---

## 4. 核心功能模块与交互细节

### 4.1 认证与安全登录
- **固定账户鉴权**：系统预设 FAE 专属访问权限（账号：`Roben`，密码：`znuobin`）。
- **记住我 (Remember Me)**：
  - 勾选：写入 `localStorage.fae_is_logged_in`，长期有效；
  - 未勾选：写入 `sessionStorage.fae_is_logged_in`，浏览器标签关闭即失效。
- **壁纸联动**：登录界面实时同步用户最新自定义桌面壁纸，呈现极致 macOS 磨砂质感。

---

### 4.2 双视界切换引擎 (Gantt View vs. Excel View)

#### A. 甘特图视界 (Gantt Timeline Engine)
1. **时间维度切换**：
   - **日视图 (Day)**：精确按单日刻度排列；
   - **周视图 (Week)**：按年周（如 W01、W02...）刻度组织，适合中期开发排期；
   - **月视图 (Month)**：宏观按月份排期，适合跨季度 MP 大里程碑。
2. **无级缩放引擎 (Zoom Engine)**：
   - 自由缩放区间 `0.6x ~ 1.5x`，刻度与任务条像素动态自适应，缩放比例自动持久化。
3. **可拖拽侧边栏 (Resizable Sidebar)**：
   - 支持鼠标按住边缘自由拉伸左侧树状层级宽度（`240px ~ 600px`），自适应超长客户与项目名。
4. **一键展开/折叠**：
   - 支持单个层级树形折叠；
   - 顶部提供「全部展开 / 全部折叠」快捷动作，搜索状态下自动展开所有匹配项。
5. **任务状态与 Blocker 警示**：
   - `risk` 任务显示风险警示标，鼠标悬浮即刻弹出带半透明磨砂的受阻详情气泡（Blocker Tooltip）；
   - 今日红线基准（Today Line）自动高亮贯穿当前日期时间刻度。
6. **快捷复制 (Quick Copy)**：
   - 单击客户名、项目名、料号名等，一键复制文本至剪贴板，伴随顶部浮动 Toast 提示。

#### B. Excel 全景表格视界 (Excel View Engine)
1. **行内无刷新编辑 (Inline Editing)**：
   - 点击单元格直接输入，失去焦点或按回车即时自动保存，支持数据自动提示补全。
2. **Updated 扩展弹窗编辑**：
   - 单元格内展示多行折叠摘要；
   - 双击或点击放大镜图标唤起 macOS 质感浮窗，支持全量 Markdown/纯文本多行编辑与查阅。
3. **多维组合筛选与单列排序**：
   - 针对销售、客户、产品线、状态等提供即时搜索过滤；
   - 支持升序/降序单列重排。
4. **行级插入与快捷删除**：
   - 支持在任意客户行之后快捷「插入新行」，快速录入新客户商机。

---

### 4.3 全局多维度筛选与概览看板 (Overview Dashboard)

1. **组合过滤器 (Filter Bar)**：
   - 风险任务过滤（一键高亮出所有带 risk 状态的任务）；
   - 产品线过滤（按 MCU / Power 等快速下钻）；
   - 商机状态过滤（Leads ~ DLOST 漏斗阶段选择）；
   - 业务员过滤（按指定 Sales 过滤名下客户）。
2. **总览模态看板 (Overview Modal)**：
   - **核心 KPI 统计卡片**：总客户、总项目、总料号、总任务、风险任务数、本周待办数；
   - **商机状态管道漏斗**：各阶段数量与占比柱状图；
   - **细分市场分布图**：Auto、Consumer、Industrial 等占比；
   - **各产品线负载分析**：直观展示哪些产品线活跃度最高；
   - **最近活动时序流**：最近编辑过的项目、料号与最新任务进展。

---

### 4.4 macOS 拟态桌面层与个性化系统 (Mac Desktop Layer)

1. **macOS 顶栏状态指示**：
   - 左侧：苹果 Logo 菜单、系统关于面板、快捷导出/导入触发；
   - 右侧：实时走字时钟、壁纸库切换开关、强调色调色盘、玻璃透明度调节开关。
2. **六大精心调配的主题强调色 (Accent Color Themes)**：
   - **白曜微光 (Pure White)**：极致纯净，雪夜通透微光（`#FFFFFF`）
   - **香槟琥珀 (Champagne Amber)**：冷暖相映，名表与温润金光（`#F59E0B`）
   - **极光薄荷 (Aurora Mint)**：欧若拉青绿，柔和清爽护眼（`#10B981`）
   - **暮光紫鸢 (Twilight Iris)**：星空晚霞交融，艺术现代感（`#A855F7`）
   - **冰川天青 (Glacier Sky)**：高山冰川晶莹澄澈，清爽明朗（`#38BDF8`）
   - **落霞珊瑚 (Sunset Rose)**：夕阳余晖温柔玫瑰，暖意典雅（`#FB7185`）
   - *全局效果*：一键切换后，按钮、聚焦高亮光晕、状态徽章、悬浮边框全量无缝响应。
3. **液态流光玻璃调光引擎 (Liquid Glass Opacity)**：
   - 支持在 `2.5% ~ 40%` 通透度范围内通过滑块微调背景磨砂通透度；
   - 自动配备「文字抗视差护盾」，确保在高通透或复杂壁纸背景下依然达到 WCAG 4.5:1 的清晰可读性。
4. **六槽位自定义壁纸库 (Custom Storable Wallpapers)**：
   - **容量限制**：最多存储 6 张专属壁纸，带有 `X / 6 已存储` 实时计数指示；
   - **点击即切**：点击任意壁纸卡片即刻切换当前桌面；
   - **设为默认**：点击星标「设默认」，将喜爱的壁纸设为开机默认启动壁纸；
   - **单卡替换**：支持对单张壁纸点击「替换」，直接上传新图覆盖对应槽位；
   - **安全删除**：支持单个壁纸删除（自动保护最后 1 张底线防黑屏）；
   - **客户端画质压缩引擎 (`compressImageFile`)**：
     - 上传图片自动经由 HTML5 Canvas 进行自适应降采样（最大 1920x1080）与 JPEG 智能压缩（质量 0.82），彻底解决浏览器 `localStorage` 5MB 配额溢出闪退的隐患。

---

## 5. 数据持久化与容错迁移机制

系统建立了一套健壮的客户端持久化方案：

```
                    [App 启动]
                        │
         ┌──────────────┴──────────────┐
         ▼                             ▼
   从 IndexedDB 获取              若 IndexedDB 为空
  (idb-keyval 主存储)            读取 localStorage 备份
         │                             │
         │                             ▼
         │                       将旧数据迁移写入
         │                          IndexedDB
         └──────────────┬──────────────┘
                        │
                        ▼
            [执行 sanitizeLoadedCustomers]
       1. 修复历史遗留的 NA 产品线 (从 rawData 重新提取)
       2. 清洗误存入任务列表的 Call Report / Weekly update
       3. 动态聚合 Project ltrAmt 金额
                        │
                        ▼
                 [写入 React State]
                        │
               [后续任何数据变更]
       以防抖/响应式自动同步写入 IndexedDB
```

- **键名定义**：
  - `fae_gantt_data_v3`：四级客户业务主数据；
  - `fae_is_logged_in`：登录鉴权状态；
  - `fae_zoom`：甘特图缩放级别；
  - `fae_sidebar_width`：甘特图左侧树栏宽度；
  - `macos_accent_color`：当前选中的系统强调色；
  - `macos_custom_wallpaper`：当前激活的桌面壁纸 Base64 或 URL；
  - `macos_user_wallpapers_v2`：6 槽位壁纸库元数据数组。

---

## 6. 潜在 Bug 审查与边缘场景排查清单

供审核与日常测试的详细检查点清单（Checklist）：

| 检查大类 | 检查项 / 潜在边界 | 预期正确行为 / 代码防线 | 对应代码位置 |
| :--- | :--- | :--- | :--- |
| **Excel 导入** | 表格无 `Product line`，但有 `Brand` 列且值为 `NA` | 产品线应保持为空，绝不能被填入 "NA"；若有 `Brand (Product line)` 则优先取之。 | `excelService.ts: extractProductLineFromRow` |
| **Excel 导入** | 单元格包含多行换行的 Call Report 或 Weekly update | 应存入 `PN.updated`，绝不能生成数十个虚假甘特图 Task 导致时间轴崩溃。 | `excelService.ts: 872-968` |
| **Excel 导入** | 导入繁体中文字符的表格（如台湾/香港原厂表格） | 自动通过 `OpenCC` 转换成简体中文存储，避免由于繁简差异导致搜索不到。 | `excelService.ts: 413-424` |
| **Excel 导入** | 客户中英文名称混杂，表头只写了 `Customer` | 检测文本内容，含汉字赋给 `nameZh`，纯英文赋给 `nameEn`，互作保底不留空。 | `excelService.ts: 479-505` |
| **Excel 导入** | 日期格式为 8 位紧凑数字 `20260115` 或带斜杠点号 `2026.1.15` | 正则自动解析并补零归一化为标准的 `2026-01-15`。 | `helpers.ts: normalizeDateStr` |
| **Excel 导入** | 表格内出现同一料号同一日期的重复任务 | 任务去重过滤器 `uniqueTasks` 自动丢弃完全重复项。 | `excelService.ts: 1038-1046` |
| **金额计算** | 金额字符串带有 `$`, `¥`, 逗号空格或 `K`, `M` 单位 (如 `$2.5M`) | `parseAmount` 自动换算为 2,500,000，并正确加总到父级 Project `ltrAmt`。 | `helpers.ts: parseAmount` |
| **壁纸存储** | 用户连续上传多张 4K/8K 超大壁纸（如原图单张 15MB） | Canvas 客户端自动等比缩小至 1080P 内并压缩，严格防止 `QuotaExceededError`。 | `MacDesktopLayer.tsx: compressImageFile` |
| **壁纸删除** | 用户删空所有壁纸 | 删除按钮在只有 1 张壁纸时禁用，若删除了当前正激活的壁纸，自动优雅回退至默认壁纸。 | `MacDesktopLayer.tsx: handleDeleteWallpaper` |
| **树状折叠** | 用户在搜索框输入关键词过滤时 | 折叠状态自动被临时覆盖为「全部展开」，防止匹配到的子项因父级折叠被隐藏。 | `App.tsx: isItemExpanded` |
| **数据安全性** | 清空全部数据动作 | 提供二次危险警告确认弹窗，必须用户明确确认后才执行 `del(STORAGE_KEY)`。 | `App.tsx: showClearConfirm` |

---

> 如在业务使用或代码审核中发现任何不符合预期的对应关系，可对照上述第 3 节的表头字典与第 6 节的排查清单快速定位！
